# Fabric-managed Git directory

Fabric workspace Git integration targets this directory. Do not place credentials or exported notebook output here. Items committed by Fabric are reviewed with `tools/security_scan.py` before the repository is public.
